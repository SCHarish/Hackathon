@using System.Configuration;
@{
    String getalllocations = ConfigurationManager.AppSettings["getallzones"];
}
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script src="~/scripts/jquery-1.10.2.js" type="text/javascript"></script>
    <script src="~/scripts/bootstrap.min.js" type="text/javascript"></script>
    <link rel="stylesheet" type="text/css" href='~/Content/Title.css'>
    <link href="~/Content/animatedtext.css" rel="stylesheet" />
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <style>
        canvas {
            width: 100%;
            height: 100%;
        }

        #watermark {
            position: relative;
            top: 50px;
            left: 0px;
            right: 0px;
            bottom: 600px;
            text-align: center;
            opacity: 0.5;
            z-index: 99;
            color: black;
        }
    </style>
</head>
<body>
    <img id="i1" src="~/Pictures/1.png" width="1px" height="1px" />
    <img id="i2" src="~/Pictures/2.png" width="0px" height="0px" />
    <img id="i3" src="~/Pictures/3.png" width="0px" height="0px" />
    <img id="i4" src="~/Pictures/feed.png" width="0px" height="0px" />
    <script>
        $(document).ready(function () {
            var check = localStorage['zone'];
            if(check==null)
            {
                //alert(check);
                alert("Please configure your location in admin mode");
            }
            else {
                //alert(check);
            }
            $("#d1").click(function () {
                var url = '@Url.Action("qrscan", "Home")';
                window.location.href = url;
            });
        }

          );
    </script>
    <center>
        <div id="d1" class="container" style="border:solid red 0px">
            <!--<div class="cssload-preloader" style="border:solid black 0px">
                <div class="cssload-preloader-box" style="border:black 0px">             <div>F</div>              <div>E</div>         <div>E</div>               <div>D</div>         <div>B</div>         <div>A</div><div>C</div> <div>K</div></div>
            </div>-->



            <div class="row col-sm-12">
                <div class='content'>
                    <div class='visible'>
                        <p>
                            Hello
                        </p>
                        <ul>
                            <li>Please Provide Feedback !</li>
                            <li>It improves our Service !</li>
                            <li>Your Opinion is very important to Us !</li>

                            <li>Thank you !</li>
                        </ul>
                    </div>
                </div>

            </div>

            <!--<div id="d1" style="position:absolute;left:10px;top:0px;height:800px;width:1290px;border:solid black 1px">-->
            <!--  <div class="col-sm-6 col-sm-offset-3" style="border:solid black 1px"></div>-->

            <canvas id="canvas" style="border:solid black 0px;margin-top:50px"></canvas>

        </div>
    </center>

    <script>

var canvas = document.getElementById('canvas');
var ctx = canvas.getContext('2d');
canvas.height = 600;
canvas.width = 1000;

var raf;

var ball = {
  x: 0,
  y: 0,
  vx: 2,
  vy: 1,

};
var ball1 = {
    x: 200,
    y: 500,
    vx: 2,
    vy: 1,
}
var ball2 = {
    x: 500,
    y: 0,
    vx: 2,
    vy: 1,
}
var ball3 = {
    x: 700,
    y: 200,
    vx: 2,
    vy: 1,
}

    var ctx=canvas.getContext("2d");
    var img = document.getElementById("i1");

    var img1 = document.getElementById("i2");

    var img2 = document.getElementById("i3");

        var img3 =document.getElementById("i4")

function draw() {
  ctx.clearRect(0,0, canvas.width, canvas.height);
  //ball.draw();
  ball.x += ball.vx;
  ball.y += ball.vy;

if (ball.y + ball.vy > canvas.height || ball.y + ball.vy < 0) {
  ball.vy = -ball.vy;
}
if (ball.x + ball.vx > canvas.width || ball.x + ball.vx < 0) {
  ball.vx = -ball.vx;
}
ball1.x += ball1.vx;
ball1.y += ball1.vy;
if (ball1.y + ball1.vy > canvas.height || ball1.y + ball1.vy < 0) {
    ball1.vy = -ball1.vy;
}
if (ball1.x + ball1.vx > canvas.width || ball1.x + ball1.vx < 0) {
    ball1.vx = -ball1.vx;
}
ball2.x += ball2.vx;
ball2.y += ball2.vy;
raf = window.requestAnimationFrame(draw);
if (ball2.y + ball2.vy > canvas.height || ball2.y + ball2.vy < 0) {
    ball2.vy = -ball2.vy;
}
if (ball2.x + ball2.vx > canvas.width || ball2.x + ball2.vx < 0) {
    ball2.vx = -ball2.vx;
}
ball3.x += ball3.vx;
ball3.y += ball3.vy;

if (ball3.y + ball3.vy > canvas.height || ball3.y + ball3.vy < 0) {
    ball3.vy = -ball3.vy;
}
if (ball3.x + ball3.vx > canvas.width || ball3.x + ball3.vx < 0) {
    ball3.vx = -ball3.vx;
}
//ctx.drawImage(img, ball.x, ball.y);
//ctx.drawImage(img1, ball1.x, ball1.y);
//ctx.drawImage(img2, ball2.x, ball2.y);
//ctx.drawImage(img3, ball3.x, ball3.y);
var x = canvas.width / 2;
var y = canvas.height / 2;
ctx.font = '35pt Calibri';
ctx.textAlign = 'center';
ctx.fillStyle = 'green';
ctx.fillText('TOUCH HERE TO GIVE FEEDBACK', x, y);
//ctx.font = '30pt Calibri';
//ctx.textAlign = 'center';
//ctx.fillStyle = 'lightgrey';
//ctx.fillText('TOUCH HERE', x, y);
}

//canvas.addEventListener('mouseover', function(e){
  raf = window.requestAnimationFrame(draw);
//});

//canvas.addEventListener("mouseout",function(e){
  //window.cancelAnimationFrame(raf);
//});

/*ball.draw();
ball1.draw();
ball2.draw();
ball3.draw();*/


    </script>



   <!-- <div style="position:absolute; bottom:0; right: 0; padding:10px;"><img src="~/Pictures/powered.gif" /></div>-->
    <div style="position:absolute; bottom:0; right: 0; padding:10px;">© 2016 powered by Honeywell</div>


</body>
</html>
